name: nf-reclaim

channels:
    - conda-forge
    - bioconda

dependencies:
    - python==3.12
    - pip
    - conda-forge::jq
    - conda-forge::duckdb-cli
    - bioconda::diamond
    - bioconda::taxonkit
    - pip:
        - "carabiner-tools[pd,mpl]>=0.0.5.post3"
        - "numpy"
        - "scikit-learn"
        - "schemist>=0.0.6"
        - "seaborn"
        - "umap-learn>=0.5.7"
        